const notas = [10, 6, 8]; //array inicial

notas.push(7); //incluindo novo item no array

const media = (notas[0] + notas[1] + notas[2] + notas[3]) / notas.length // chamando os itens do array que já será atualizado plo método push

console.log(media);